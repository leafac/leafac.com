{
  "scripts": {
    "start": "concurrently 'nodemon --ext md,js build.js' 'live-server --wait=2000'",
    "build": "node build.js",
    "test": "npm run build && prettier --check '**/*.{md,js,css}'"
  },
  "dependencies": {
    "concurrently": "^5.2.0",
    "glob": "^7.1.6",
    "jsdom": "^16.2.2",
    "katex": "^0.11.1",
    "live-server": "^1.2.1",
    "marked": "^0.8.2",
    "nodemon": "^2.0.3",
    "parse-numeric-range": "^1.2.0",
    "prettier": "^2.0.5",
    "shiki": "^0.1.7"
  }
}
